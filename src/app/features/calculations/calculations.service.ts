/** Re-export do serviço migrado para core/services. Mantido para compatibilidade de imports existentes. */
export { CalculationsService } from '../../core/services/calculations.service';
export type { Calculation, CalculationPayload, CalculationType } from '../../core/models/calculation.model';
