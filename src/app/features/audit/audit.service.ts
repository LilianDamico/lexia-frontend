/** Re-export do serviço migrado para core/services. Mantido para compatibilidade de imports existentes. */
export { AuditService } from '../../core/services/audit.service';
export type { AuditEvent } from '../../core/models/audit.model';
